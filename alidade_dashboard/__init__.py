"""Alidade dashboard frontend bundle.

The package contains no Python — `alidade_dashboard.static` holds the built React app served by the Go dashboard binary, and `alidade_dashboard.config` holds the colors.json the binary reads at startup.
"""
