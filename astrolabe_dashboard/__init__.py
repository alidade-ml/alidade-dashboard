"""Astrolabe dashboard frontend bundle.

The package contains no Python — `astrolabe_dashboard.static` holds the built React app served by the Go dashboard binary, and `astrolabe_dashboard.config` holds the colors.json the binary reads at startup.
"""
